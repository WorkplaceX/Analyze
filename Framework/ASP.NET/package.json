{
  "version": "1.0.0",
  "name": "asp.net",
  "dependencies": {
    "aspnet-prerendering": "1.0.7"
  },

  "scripts": {
    "installAll": "npm install && cd ../Angular/ && npm install && npm run tsc && cd \"../Angular Universal/\" && npm install && cd \"../ASP.NET/Application/Node.js/Server/\" && npm install && cd ../../../ && npm run buildAll",
    "buildAll": "cd \"../Angular Universal/\" && npm run build:prod && cd ../ASP.NET/ && node FileCopy.js"
  }
}