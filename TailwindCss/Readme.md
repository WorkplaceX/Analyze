# Tailwind Css
Packages:
* autoprefixer
* postcss
* postcss-loader
* tailwindcss

Switch to minify (tree shaking). File: tailwind.config.js
* enabled: true

´´´cmd
npm install
npm run build // See output folder dist/
´´´
